# Public runtime config. Values are exposed in dev as:
#   {{ CATALOG_API_URL }}
# The first slice uses bundled catalog metadata; this path is the planned
# read-only service boundary.
CATALOG_API_URL=/v1/catalog
